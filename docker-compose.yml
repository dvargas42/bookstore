version: '3.8'

services:
  mysql:
    image: mysql:5.7
    container_name: bookstoredb
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: bookstoredb
    ports:
      - "3306:3306"
 